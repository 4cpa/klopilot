# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
.expo/
out/
build/

# TypeScript incremental build info
*.tsbuildinfo

# Turborepo
.turbo/

# Test coverage
coverage/

# Environment — NEVER commit real secrets
.env
.env.local
.env.*.local
.env.prod          # production secrets — nur auf dem Server, nie im Repo
# .env.prod.example ist committed (kein Secret, nur Template)

# Editor
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Prisma — Migrations-Ordner wird versioniert (enthält migration.sql-Dateien)
# apps/api/prisma/migrations/  ← absichtlich NICHT ignoriert

# Mobile
*.orig.*
web-build/
